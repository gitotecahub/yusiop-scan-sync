const TopBar = () => {
  return (
    <div className="flex items-center justify-center p-4 bg-background/95 backdrop-blur-sm border-b border-border/50">
      <h1 className="text-lg font-bold bg-gradient-to-r from-primary to-primary/60 bg-clip-text text-transparent">
        Yusiop
      </h1>
    </div>
  );
};

export default TopBar;